<!--
REVISION — an updated version of a prior move you authored. Does NOT edit
the prior move; supersedes it. The prior move stays in the file; the
revision is appended below. The validator marks the prior as superseded by
the new move's reference.

Use REVISION when:
- New information arrived that changes your position
- A critique surfaced an error in your reasoning
- You want to tighten the move without restating from scratch

Do NOT use REVISION to edit someone else's move (you can't), or to silently
walk back a position (be explicit about what changed).
-->
### [REVISION] @<handle> · <ISO-8601-timestamp> → supersedes <MOVE_TYPE>@<handle>#<deliberation_id>

## What changed

<!-- The specific changes from the prior version. Concrete:
     - Position now reads "X" instead of "Y"
     - Risks: added "rate-limit churn"
     - Removed Alternative C (no longer applicable after DECISION@.../#0011)
   -->

## Why it changed

<!-- The reason for the revision. Cite the move(s) or new information that
     prompted it. -->

## Revised move

<!-- The full revised move, in the same format as the original move type
     (PROPOSAL, CRITIQUE, etc.). All required sections must be present. -->
